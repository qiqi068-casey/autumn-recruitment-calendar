# GitHub Pages 发布说明

1. 在 GitHub 新建一个公开仓库，推荐命名为 `autumn-recruitment-calendar`。
2. 将本项目推送到仓库的 `main` 分支。
3. 打开仓库的 **Settings → Pages**。
4. 在 **Build and deployment** 下把 **Source** 设为 **GitHub Actions**。
5. 打开仓库的 **Actions** 页面，等待 `Deploy GitHub Pages` 运行完成。
6. 网站地址通常为 `https://你的GitHub用户名.github.io/autumn-recruitment-calendar/`。

以后每次向 `main` 分支推送代码，网站都会自动更新。
